# Бриф для фронтенд-агента: Qos Perne → стиль NIS

Ты верстаешь лендинг школьного домбыра-клуба **Qos Perne** (школа NIS).
Перечитай контракт стиля: **`STYLE_GUIDE.md`** (v2) — он главный.
Ниже — только то, что нужно сделать прямо сейчас.

## Направление

Серьёзный, приземлённый, «корпоративный» сайт **по образцу nis.edu.kz**.
Разбор реального дизайна NIS уже сделан — он в `STYLE_GUIDE.md`, разделы 0 и 0.5.

Коротко:
- **Фон светлый** (`#f0f2f5` / белый), текст тёмный (`#1a1a1a`). Не тёмная тема.
- **Один акцент — зелёный NIS:** база `#80c342`, сильный/hover `#67a600`, soft `#eaf5dc`.
  WhatsApp-кнопка — `#25d366`.
- **Шрифты только sans:** Inter (заголовки, UI) + Manrope (текст). Serif/Playfair убрать.
- **Кнопки — pill** (`border-radius: 999px`), карточки — белые, `radius 10px`,
  `1px solid #e5e7eb`, **без теней и без glow**.
- **Контейнер 1200px**, секции чередуются белый / `#f0f2f5`, отступы 64–96px.

## Анимации — минимум (этого хватает)

Разрешено только:
- hover/focus: transition `150–200ms` (цвет/фон/рамка/opacity);
- открытие меню/модалки: fade + `translateY(8px)`, `200ms ease-out`;
- опционально: одноразовый fade-up секции при скролле (`300ms`).

**Убрать полностью:** glow / colored shadows / неон, blur-подложки,
noise/grain-оверлеи, бегущие строки (marquee), конфетти, параллакс,
вибрация струн, вращения, анимированные градиенты, shimmer.
`prefers-reduced-motion` — всё выключать.

## Что поправить в текущем коде (чек-лист)

Сейчас в `src/` скорее всего ещё старая «максималистичная» тема. Пройдись по ней:

- [ ] `src/index.css`: тёмный `--bg` → `#f0f2f5`; убрать radial-gradient «атмосферы»
- [ ] убрать `.noise-overlay` (если есть)
- [ ] убрать marquee/бегущую строку
- [ ] убрать confetti в `RegistrationModal`
- [ ] gold-gradient тексты (`.title-accent`, `em`) → обычный цвет
- [ ] glow-тени карточек/кнопок → убрать (или `0 1px 2px rgba(0,0,0,.04)`)
- [ ] тёмная backdrop-blur шапка → белая шапка, `border-bottom: 1px solid #e5e7eb`
- [ ] serif в заголовках → Inter 700/800
- [ ] hero с тёмной вуалью на фото → светлый hero: чёрный h1 (акцентное слово
      зелёное `#67a600`), подзаголовок, 2 CTA, фото-баннер **под** текстом без вуали
- [ ] rotating crest / string-vibrate в `DombraTuner` → ordinary hover/focus

## Идентичность Qos Perne (не потерять характер клуба)

См. `STYLE_GUIDE.md`, раздел **0.5**. Суть:
- Реальные **фото ансамбля** — главный носитель души; логотипы `qos_perne_logo` + `logo_NIS` всегда парой.
- Казахский **орнамент — статичной линией** (1px, `#d1d5db`/зелёный), не анимированный и не светящийся.
- Слова-якоря: қос перне, қос ішек, күй, төкпе/шертпе, домбыра, ансамбль, сахна.
  Заголовки секций — по-казахски + короткий русский подзаголовок.
- Лозунг «Қос перне • Қос ішек • Бір жүрек» — спокойно, без анимации (hero/footer).
- Тёплые подписи допустимы («Домбыраны мектеп береміз»), UI — нейтральный.

## Данные — честно

- `src/data/achievementsData.js`: текущие призы («Гран-при Winter Ball»,
  «I Дәрежелі Лауреат», «Ұлы Дала Сазы», «Диплом Баку») — **выдуманы, это моки**.
  Пока владелец не подтвердил — помечай в коде комментарием `// MOCK` и не выдавай за факт.
  Hero-надпись «Халықаралық жеңістер» — тоже не подтверждена, лучше нейтральнее
  («Жетістіктер» / «Біздің сахналар»).
- `src/data/repertoireData.js`: классы участников не выдумывать; часть реальных
  есть в `landing/dombyra-main/repertoire.json` — сверяться с ним.

## Ссылки

- WhatsApp группы (везде одна): `https://chat.whatsapp.com/FAgVLbxew4SEcbsksGsS4g`
- Референсы: https://www.nis.edu.kz/ru , https://schools.nis.edu.kz/ru
- Старый сайт-база репертуара: `landing/dombyra-main/` (его `public/index.html`
  тоже должен соответствовать `STYLE_GUIDE.md`)

## Доне, когда

1. Все пункты чек-листа выше закрыты.
2. `npm run build` проходит без ошибок.
3. Страница выглядит как светлый NIS-корпорат: белый/серый, зелёный акцент,
   sans, pill-кнопки, ноль glow и шума, анимации ≤200ms.
4. Сверил с `STYLE_GUIDE.md` — там всё расписано подробнее.
