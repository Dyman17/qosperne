# Qos Perne — Style Guide v2 (направление: NIS Corporate)

Единый контракт стиля для всех страниц: новый React-лендинг (`src/`),
старый сайт-база (`landing/dombyra-main/public/`), будущие формы и кабинет учителя.
Кто правит фронт — сверяется с этим файлом. v1 (тёмное+золото+глоу) — **отменена**.

---

## 0.5. Идентичность Qos Perne — что обязано остаться своим

Строгость NIS — это рамка, а не обезличивание. Qos Perne не «ещё один школьный
сайт»: это клуб домбыры, и по нему должно быть видно, о чём он, уже за 3 секунды.

**Характер:** мы — молодой, но серьёзно относящийся к своему делу ансамбль при NIS.
Тон сайта: уверенный, тёплый, уважительный к традиции, но без пафоса и без «лубка».
Пишем короткими честными фразами. Казахский — первый язык, русский — пояснение.

**Как передаём принадлежность к клубу (в пределах гайда):**

1. **Фотографии — главный носитель души.** Реальные снимки ансамбля (сцена, Баку,
   Winter Ball, репетиции) вместо стоков и иллюстраций. Фото — чистые, без тёмных
   вуалей и цветных фильтров; эмоцию несёт кадр, а не обработка.
2. **Логотипы рядом:** `qos_perne_logo` + `logo_NIS` — всегда в паре, так же, как
   NIS ставит орнамент рядом с навигацией. Фавикон — логотип клуба.
3. **Казахский орнамент — статичной линией, не глоу.** Допустим тонкий
   разделитель-орнамент (1px линия, `--brand` или `#d1d5db`) между секциями и
   в подвале — как у NIS `ornament.png` в шапке. Никаких анимированных,
   светящихся, вращающихся орнаментов.
4. **Слова-якоря в текстах:** қос перне, қос ішек, күй, төкпе/шертпе, домбыра,
   ансамбль, сахна. Заголовки секций — по-казахски, с коротким русским подзаголовком.
   Лозунг «Қос перне • Қос ішек • Бір жүрек» — в футере/hero, набранный спокойно,
   без анимации.
5. **Зелёный NIS + характер клуба через контент, не через палитру.** Национальный
   колорит дают фото, орнамент-линия, казахские слова и реальные истории
   (победы, поездки, кюй) — а не золото, неон и узоры на кнопках.
6. **Приземлённая «клубность» вместо корпоративной сухости:** в блоках допустимы
   тёплые человеческие подписи («Домбыраны мектеп береміз», «Нөлден үйретеміз»),
   но UI-элементы остаются нейтральными и аккуратными.
7. **Числа и факты — настоящие** (состав групп, число кюйев, реальные награды
   после подтверждения). Выдуманные цифры и призы характера клубу не добавляют —
   они его портят.

**Коротко:** рамка — NIS (светлое, зелёное, sans, тихо); содержание и лицо —
Qos Perne (фото своего ансамбля, орнамент-линия, казахский, кюй, честные факты).

---

## 0. Откуда взят стиль (глубокий анализ NIS)

Разобран живой продакшн **nis.edu.kz** и **schools.nis.edu.kz** (Tailwind v4 + Alpine.js,
продакшн-CSS `build/assets/app-*.css`, HTML шапки/футера/секций).

**Что делает NIS «серьёзным и приземлённым»:**

1. **Светлая тема по умолчанию.** Белый/светло-серый фон (`#fff`, `#f0f2f5`), тёмный текст.
   Никаких «премиальных» тёмных экранов. Тёмное — только локально (иконки, логотип `#080341`).
2. **Один бренд-акцент — зелёный.** `bg-primary` встречается в HTML 74 раза, `bg-secondary` 20,
   `border-primary` 18. Пара зелёного: база `#80c342`, ховер/сильный `#67a600`,
   светлый `#a0d857`. Зелёный — это CTA-кнопки, активные пункты меню, ховеры ссылок.
   Больше акцентных цветов нет (синий/голубой — только иконки соцсетей).
3. **Типографика — только sans-serif.** `Instrument Sans` (основной `--font-sans`),
   `SF Pro Display` (заголовки, `font-sf`), `Inter` (Regular/Medium/SemiBold).
   Ни одного serif/декоративного шрифта. Иерархия — размером и весом, не курсивом.
4. **Радиусы — спокойные, кнопки — «пилюли».** Доминирует `--radius-3xl: 1.5rem`
   (массивные `rounded-3xl` у пунктов меню и CTA), карточки/инпуты — `10px`,
   у части блоков — `0` (честные прямые углы). Смесь pill-кнопок и прямоугольных карточек.
5. **Тени — почти нет.** Весь CSS по сути сводится к Tailwind-переменным `box-shadow`
   и одному `shadow-lg` у выпадающих меню (`ring-1 ring-black/5` — кольцо в 5% чёрного).
   Никаких coloured glow-теней, свечений, неона.
6. **Анимации — минимальные и функциональные.** Весь продакшн-CSS содержит ровно
   3 keyframes (`spin`, `ping`, `pulse` — стоковые Tailwind) и переходы `150–300ms`.
   Меню/дропдауны: fade + сдвиг `translate-y-2`, `duration-200`, `ease-out`.
   `animate-fade-in-down` — одна кнопка-баннер. Всё. Ни параллакса, ни бегущих строк,
   ни частиц, ни blur-фонов.
7. **Шапка — двухъярусная, sticky, белая.** `nav.sticky.top-0.bg-white.z-50`:
   верх (анонс-пилюля, поиск, телефон, версия для слабовидящих, KZ/RU/EN),
   низ (орнамент-логотип, пункты меню с `rounded-3xl` ховером, новости/вакансии).
   На мобильном — бurger + выдвижное меню.
8. **Hero — редакционный, не «карусельный».** Крупный чёрный заголовок в 3 строки
   (`text-6xl/7xl`), короткий подзаголовок, широкое фото-полотно под ним
   (`banner_header.webp`), затем лента партнёров логотипами. Никакого текста поверх фото с глоу.
9. **Секции-паттерны:** лента новостей (картинка + заголовок + дата), сетка
   «иконка + заголовок + абзац» (экосистема/ресурсы), блок «Цифры и факты»
   (огромные числа `text-5xl/6xl` + подпись, фон — спокойная texture-фото),
   FAQ-аккордеон, футер `bg-gray-100` колонками (документы, адрес, горячая линия, соцсети).
10. **Плотность информации выше украшательства.** Много текста, чёткие разделители,
    стандартные сетки, max-width контейнера `72–80rem` (1152–1280px).

**Вывод для Qos Perne:** серьёзность NIS = светлый фон + один зелёный акцент +
sans-serif + прямые карточки с 1px-рамкой + тихие 150–200ms переходы. Не «праздник
для инстаграма», а «официальный сайт организации, которому можно верить».

---

## 1. Принпы (в порядке важности)

1. Светлое, чистое, информационное. Контент — герой; украшений — минимум.
2. Ровно один смысловой акцентный цвет на весь сайт — зелёный NIS.
3. Ноль glow, неона, colored-shadow, blur-подложек, шумовых оверлеев.
4. Анимация — только функциональная (feedback ховера/открытия), 150–200ms.
5. Шрифты — sans-serif, иерархия размером/весом.
6. Казахский — язык заголовков и основного текста; русский — подписи/пояснения.

## 2. Палитра

| Токен | Значение | Назначение |
|---|---|---|
| `--bg` | `#f0f2f5` | фон страницы (их серый) |
| `--surface` | `#ffffff` | карточки, шапка, секции |
| `--surface-alt` | `#f8f9fa` | чередующиеся секции |
| `--border` | `#e5e7eb` | рамки карточек, разделители (1px) |
| `--text` | `#1a1a1a` | основной текст, заголовки |
| `--text-2` | `#535b5e` | вторичный текст |
| `--text-muted` | `#919191` | подписи, плейсхолдеры |
| `--brand` | `#80c342` | бренд-зелёный (заливки, пилюли) |
| `--brand-strong` | `#67a600` | ховер, акцентные слова, CTA |
| `--brand-soft` | `#eaf5dc` | светлая подложка чипов/тегов |
| `--navy` | `#080341` | логотип/глубокий тёмный акцент (лаконично) |
| `--wa` | `#25d366` | только кнопка WhatsApp (их паттерн) |

Градиенты в UI **не использовать** (кроме, при необходимости, лёгкого белого overlay на фото).
Фон hero — белый/`--surface`, не тёмный.

## 3. Типографика

- Шрифты: **Inter** (заголовки 700/800, UI 500/600) + **Manrope** (текст 400/500)
  — уже есть в проекте. Serif (Playfair) — **не использовать**.
- Шкала (совпадает с NIS `--text-*`):
  - h1: `clamp(2.5rem, 6vw, 4.5rem)` (≈ text-6xl/7xl), line-height 1.05–1.1
  - h2: `clamp(1.875rem, 4vw, 3rem)` (text-4xl/5xl)
  - h3: `1.5rem` (text-2xl/3xl)
  - body: `1rem` / `line-height 1.6`; small: `.875rem`
- Акцентное слово в заголовке — **зелёным `--brand-strong`**, не градиентным золотом.
- Заглавные с огромным трекингом (`.28em`) — не использовать; eyebrow: `.12em`, 700, зелёный.

## 4. Компоненты

- **Кнопки:** `border-radius: 999px` (их `rounded-3xl`), padding `12px 24px`,
  `font-weight: 600`, `min-height: 44px`.
  - Primary: фон `--brand-strong`, текст white, ховер — на шаг темнее (`#558c00`), без тени.
  - Secondary: белый фон, `1px solid --border`, текст `--text`, ховер — рамка `--brand-strong`.
  - WhatsApp: фон `--wa`, белый текст.
  - Переходы: `background-color, border-color, color 150ms ease`. `transform` — максимум `translateY(-1px)`.
- **Карточки:** белые, `border-radius: 10px`, `border: 1px solid --border`,
  padding `20–28px`. Тень — **не нужна** (или `0 1px 2px rgba(0,0,0,.04)` максимум).
  Ховер: рамка → `#d1d5db`. Без подъёма, без свечения.
- **Инпуты/select:** белые, `1px solid #9ca3af`, radius `8px`, height `44px`;
  focus: `border-color: --brand-strong; box-shadow: 0 0 0 3px rgba(103,166,0,.15)` (как у них
  `focus:border-2 focus:border-primary`).
- **Чипы/теги:** фон `--brand-soft`, текст `#3f6212`/`--text`, radius `999px`, без теней.
- **Фото:** radius `0` или `10px`; рамки — нейтральные `--border`, никаких золотых/зелёных обводок-свечений.
- **Модалка/дропдаун:** белая, radius `16px` (`24px` допустимо), тень `0 10px 40px rgba(0,0,0,.12)`,
  backdrop `rgba(0,0,0,.4)` без blur.
- **Секции:** контейнер `max-width: 1200px`, боковые паддинги `20–24px`,
  вертикальные отступы `64px` (моб.) / `96px` (десктоп), чередование `--surface` / `--bg`.

## 5. Анимация — жёсткая политика

**Разрешено:**
- `transition: color/background/border/opacity 150–200ms ease` на hover/focus/active.
- Открытие меню/модалки: `opacity 0→1` + `translateY(8px→0)`, `200ms ease-out`.
- Одноразовый fade-up секции при скролле (IntersectionObserver), `300ms`, `translateY(16px)`,
  одна и та же кривая — опционально, максимум на секцию один раз.
- `@media (prefers-reduced-motion: reduce)` — всё выключить.

**Запрещено (проверять при ревью каждого PR):**
- glow / colored box-shadow / неоновые обводки / `filter: blur()` подложки
- шумовые/зернистые оверлеи (`noise`, `grain`)
- бегущие строки (`marquee`)
- конфетти, частицы, эмодзи-дождь
- параллакс, бесконечные пульсации, вращения элементов, «вибрация струн»
- анимированные градиенты, shimmer, skeleton-блеск
- многослойные radial-gradient «атмосферы» на фоне

## 6. Паттерны страниц (по образцу NIS)

1. **Шапка** — sticky, белая, `border-bottom: 1px solid --border`, высота ~64–72px:
   логотип (Qos Perne + NIS) слева, навигация, справа — secondary «Тіркелу» + primary
   WhatsApp-пилюля. Моб. — burger → выдвижная панель (fade+slide 200ms).
2. **Hero** — белый фон: крупный чёрный h1 (акцентное слово зелёное), подзаголовок,
   2 CTA (primary + secondary), под ними широкое фото клуба `main.jpeg`
   (без тёмной вуали; при необходимости — лёгкий белый overlay 10–20% снизу).
   Статистика — отдельной строкой «число + подпись», sans, чёрное + зелёное.
3. **О клубе** — 2 колонки: текст + карточки-иконки (Үлкен/Кіші топ/кесте) на белом.
4. **Жетістіктер** — сетка карточек 10px/1px-рамка: медаль-чип, место, год, город.
   **Факты — только подтверждённые владельцем; сомнительное помечать «мок».**
5. **Репертуар** — «таблица-подобный» UI: поиск (их паттерн инпута), фильтры-чипы,
   список строк с разделителями, счётчики. Приземлённо, без аватарок-градиентов.
6. **Цифры и факты** — блок как у NIS: крупные числа, короткие подписи, серый фон.
7. **Галерея** — ровная сетка 16:10, radius 10px, лайтбокс без анимаций кроме fade 200ms.
8. **FAQ** — аккордеон (grid-rows 0fr→1fr, 200ms) — как у NIS.
9. **Футер** — `--bg` (`#f0f2f5`), колонки: разделы / контакты / соцсети, копирайт,
   ссылка на товарный знак NIS-принадлежности. Без тёмного футера.

## 7. Чек-лист ревью (что убрать из текущего React-фронтенда)

- [ ] `app-atmosphere` (radial-gradient зелёно-золотые подсветки) → убрать
- [ ] `noise-overlay` → убрать
- [ ] `marquee-bar` / бегущая строка → убрать
- [ ] confetti в `RegistrationModal` → убрать
- [ ] тёмный фон страницы (`--bg: #09130d`) → заменить на `#f0f2f5`, текст тёмный
- [ ] gold-gradient тексты (`.title-accent`, `em`) → plain color
- [ ] glow-тени карточек/кнопок (`box-shadow` с зелёным/золотым) → убрать
- [ ] rotating crest hover, string-vibrate → убрать
- [ ] backdrop-blur тёмная шапка → белая шапка без blur
- [ ] serif Playfair в заголовках → Inter
- [ ] hero с тёмной вуалью → светлый hero + фото-баннер

## 8. Данные и ссылки

- WhatsApp группа: `https://chat.whatsapp.com/FAgVLbxew4SEcbsksGsS4g`
- Референсы: https://www.nis.edu.kz/ru , https://schools.nis.edu.kz/ru
- Мок-достижения (`src/data/achievementsData.js`) — до подтверждения владельцем
  считать заглушками; конкретные «Гран-при / I лауреат» не выдавать за факт без проверки.
- Старый сайт-база: `landing/dombyra-main/` (стиль также приводить к этому гайду).
