# Qos Perne — описание стиля (Style Guide)

Единый визуальный язык лендинга и всех будущих страниц (включая старый сайт-базу).
Если переписываешь любой экран — сверяйся с этим файлом.

## 1. Настроение
Тёмная сцена + золото. Национальный казахский характер без лубка:
домбыра, күй, орнамент-волна, приглушённый свет софитов.
Контраст: почти чёрный фон ↔ светлое «бумажное» поле для чтения.

## 2. Палитра
| Токен | Значение | Где |
|---|---|---|
| `--bg` | `#0d0b08` | основной тёмный фон |
| `--bg2` / `--card` | `#14100b` / `#1c1610` | карточки на тёмном |
| `--paper` | `#f7f1e5` | светлый фон разделов |
| `--paper2` | `#efe5d0` | градиент светлого |
| `--gold` | `#c9a227` | акцент, рамки, бейджи |
| `--gold2` | `#e8c35a` | светлое золото, заголовки на тёмном |
| `--gold-deep` | `#8a6d14` | акцент на светлом фоне |
| `--ink` | `#1a140d` | текст на светлом |
| `--muted` | `#8b7f6a` | вторичный текст |
| `--line` | `rgba(201,162,39,.28)` | золотая рамка карточек |

Градиент золота для главных кнопок и заголовков:
`linear-gradient(135deg, #e8c35a, #c9a227 60%, #9a7a16)`.

## 3. Типографика
- Заголовки: `Playfair Display` (запасной `Marcellus`, serif), жирность 700–800,
  межбуквенный интервал у латиницы `QOS PERNE` — `+.04em`.
- Текст/кнопки: `Manrope`, системный запасной.
- Курсивный акцент в заголовках (`<em>`) — золотым цветом.
- Подзаголовки-разделы: капс, трекинг `.28em`, размер `.75rem`, жирность 800.

## 4. Компоненты
- **Кнопки** — пилюли `border-radius: 999px`, жирность 800.
  - `.btn-gold` — золотой градиент, тёмный текст, тень `0 10px 30px rgba(201,162,39,.35)`.
  - `.btn-ghost` — прозрачная с рамкой (белой на тёмном, `--ink` на светлом).
  - `.btn-dark` — заливка `--ink`, текст `--paper`.
- **Карточки на тёмном** — градиент `#221a10 → #171208`, рамка `var(--line)`,
  скругление `20px`, сверху тонкая золотая полоса `4px`.
- **Поля ввода** — пилюли, тёмная заливка `rgba(255,255,255,.07)`,
  рамка `var(--line)`, текст белый, плейсхолдер `#9b8d6f`.
- **Теги/пилюли** — рамка `var(--line)`, скругление `999px`.
- **Орнамент** — SVG-волна с ромбом, повтор по горизонтали, золото, `opacity ~.5`.
- **Бегущая строка** — плашка `var(--gold)`, тёмный текст, капс.

## 5. Фон с фото
Фото (`images/main.jpeg`) кладётся слоем с `opacity: .52` и `transform: scale(1.04)`,
поверх — вуаль: радиальный отсвет золота сверху + линейное затемнение
`rgba(13,11,8,.62) → .45 → .82`. Текст поверх всегда белый/золото.

## 6. Правила
- Никакого чистого серого `#f5f5f5`, синего `#2196F3` и зелёного `#4CAF50`
  в UI — только палитра выше (это была болезнь старого сайта).
- Никакого `Arial` — только связка Playfair Display + Manrope.
- Казахский — первый язык заголовков, русский — второй (подписи, хинты).
- Все мок-места помечать `TODO` + префикс «мок» в тексте, чтобы владелец видел,
  что заменить.
- Мобильная версия обязательна: карточки в 1 колонку, формы вертикально,
  тач-цели от `44px`.

## 7. Связанные ссылки
- WhatsApp группа: `https://chat.whatsapp.com/FAgVLbxew4SEcbsksGsS4g`
- Лендинг: `index.html` в корне. Старый сайт-база: `landing/dombyra-main/`.
